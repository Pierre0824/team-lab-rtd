# Core Sphinx extensions (keep minimal; add more only if you need them)
recommonmark

# Install F5 Sphinx theme directly from GitHub (no local path needed)
git+https://github.com/f5devcentral/f5-sphinx-theme.git@master
